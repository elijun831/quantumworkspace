version: '2.0.1'
services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
      - "5678:5678"  # Expose port 5678 for debugging
    volumes:
      - ./notebooks:/notebooks
    user: "appuser"
