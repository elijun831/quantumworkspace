version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./app:/app
      - ./notebooks:/notebooks
    ports:
      - 8888:8888
      - 5678:5678
    user: appuser
    environment:
      - PYTHONDEBUG=1
